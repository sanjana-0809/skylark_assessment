# Notebook

The end-to-end Colab notebook is `model_training.ipynb` in this folder.

It contains the full pipeline: EDA, dataset caching, training, and inference with soft-argmax decoding. Open it in Google Colab with a T4 GPU runtime, mount Drive, and run all cells.